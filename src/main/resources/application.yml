spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_repository_2
    username: fconsiglio98
    password: inserirePassword
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
server:
  port: 5050
